{
  "name": "railsim2-help-docs",
  "private": true,
  "version": "0.0.1",
  "description": "Unofficial mirror of RailSim II help documentation",
  "type": "module",
  "scripts": {
    "prebuild": "tsx scripts/process-help.ts",
    "build": "npm run prebuild && vite build",
    "dev": "npm run prebuild && vite --open",
    "preview": "vite preview"
  },
  "devDependencies": {
    "iconv-lite": "^0.6.3",
    "tsx": "^4.21.0",
    "vite": "^6.3.0"
  }
}
